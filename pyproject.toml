[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "warp-factory-py"
version = "0.2.0"

[tool.setuptools.packages.find]
include = ["warp_factory_py*"]