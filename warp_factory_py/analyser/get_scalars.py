def get_scalars(metric):

    array_metric_tensor[0, 0, 0, 0, :, :] = metric['tensor']

    array_metric_tensor = 

    alpha, _, _, beta_up, _ = 

    return expansion_scalar, shear_scalar, vorticity_scalar